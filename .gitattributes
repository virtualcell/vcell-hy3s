# Split out of virtualcell/vcell-solvers, where a mix of CRLF and LF is already
# committed. Disable end-of-line conversion so files stay byte-identical to
# their originals and diffs against the old tree remain readable.
* -text
